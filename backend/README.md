# Backend - Web SKU Identifier

## Como rodar

1. Instale as dependências:
   ```bash
   pip install -r requirements.txt
   ```
2. Execute o servidor:
   ```bash
   uvicorn main:app --reload
   ```

O backend irá expor o endpoint `/upload-image/` para receber imagens via POST.
